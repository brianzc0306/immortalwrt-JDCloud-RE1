#
# Build ImmortalWrt for JDCloud RE-CS-07
#

name: immortalwrt-nikki

on:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      build_variant:
        description: 构建类型（稳定版或 OAF 测试版）
        required: true
        default: stable
        type: choice
        options:
          - stable
          - oaf-test

permissions:
  contents: read

concurrency:
  group: immortalwrt-nikki-${{ github.ref }}-${{ github.event.inputs.build_variant || 'stable' }}
  cancel-in-progress: true

env:
  REPO_URL: https://github.com/VIKINGYFY/immortalwrt.git
  REPO_BRANCH: main
  CONFIG_FILE: nikki.config
  DIY_P2_SH: diy-part2.sh
  FIRMWARE_FLAVOR: ${{ github.event.inputs.build_variant || 'stable' }}
  OAF_REPO_URL: https://github.com/destan19/OpenAppFilter.git
  OAF_COMMIT: a189ad85e8fb461318533941963f0e2975274a19
  UPLOAD_BIN_DIR: false
  UPLOAD_FIRMWARE: true
  UPLOAD_RELEASE: true
  TZ: Asia/Shanghai

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 360
    permissions:
      contents: read
    outputs:
      release_tag: ${{ steps.release_info.outputs.release_tag }}
      release_prerelease: ${{ steps.release_info.outputs.release_prerelease }}

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Initialize environment
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo rm -rf \
            /etc/apt/sources.list.d/* \
            /usr/share/dotnet \
            /usr/local/lib/android \
            /opt/ghc \
            /opt/hostedtoolcache/CodeQL

          sudo docker image prune --all --force
          sudo -E apt-get -qq update
          sudo -E apt-get -qq install \
            ack \
            antlr3 \
            asciidoc \
            autoconf \
            automake \
            autopoint \
            binutils \
            bison \
            build-essential \
            bzip2 \
            ccache \
            cmake \
            cpio \
            curl \
            device-tree-compiler \
            fastjar \
            flex \
            gawk \
            gettext \
            gcc-multilib \
            g++-multilib \
            git \
            gh \
            gperf \
            haveged \
            help2man \
            intltool \
            libc6-dev-i386 \
            libelf-dev \
            libfuse-dev \
            libglib2.0-dev \
            libgmp3-dev \
            libltdl-dev \
            libmpc-dev \
            libmpfr-dev \
            libncurses5-dev \
            libncursesw5-dev \
            libpython3-dev \
            libreadline-dev \
            libssl-dev \
            libtool \
            lrzsz \
            mkisofs \
            msmtp \
            nano \
            ninja-build \
            p7zip \
            p7zip-full \
            patch \
            pkgconf \
            python2.7 \
            python3 \
            python3-pyelftools \
            python3-setuptools \
            qemu-utils \
            rsync \
            scons \
            squashfs-tools \
            subversion \
            swig \
            texinfo \
            uglifyjs \
            upx-ucl \
            unzip \
            wget \
            xmlto \
            xxd \
            zlib1g-dev

          sudo -E apt-get -qq autoremove --purge
          sudo -E apt-get -qq clean
          sudo timedatectl set-timezone "$TZ"
          sudo mkdir -p /workdir
          sudo chown "$USER:$GROUPS" /workdir

      - name: Clone ImmortalWrt
        working-directory: /workdir
        run: |
          df -hT "$PWD"
          git clone \
            --depth=1 \
            --branch "$REPO_BRANCH" \
            "$REPO_URL" \
            openwrt
          ln -sfn /workdir/openwrt "$GITHUB_WORKSPACE/openwrt"

      - name: Restore build cache
        uses: klever1988/cachewrtbuild@b729f807837dbc3455d02fd35192660d9b662121 # main
        with:
          ccache: "true"
          prefix: ${{ github.workspace }}/openwrt

      - name: Update feeds
        working-directory: openwrt
        run: ./scripts/feeds update -a

      - name: Install feeds
        working-directory: openwrt
        run: ./scripts/feeds install -a

      - name: Load configuration
        run: |
          if [ ! -f "$CONFIG_FILE" ]; then
            echo "ERROR: $CONFIG_FILE not found"
            exit 1
          fi

          mv "$CONFIG_FILE" openwrt/.config
          chmod +x "$DIY_P2_SH"
          cd openwrt
          echo "Build variant: $FIRMWARE_FLAVOR"
          "$GITHUB_WORKSPACE/$DIY_P2_SH"

      - name: Download packages
        working-directory: openwrt
        run: |
          make defconfig

          echo "===== Selected target ====="
          grep '^CONFIG_TARGET.*DEVICE.*=y' .config

          echo "===== Required acceleration and proxy path ====="
          for required_config in \
            CONFIG_PACKAGE_kmod-qca-nss-drv=y \
            CONFIG_PACKAGE_kmod-qca-nss-ecm=y \
            CONFIG_PACKAGE_kmod-nft-tproxy=y
          do
            if ! grep -qx "$required_config" .config; then
              echo "ERROR: required config missing: $required_config"
              exit 1
            fi
            echo "$required_config"
          done

          if [ "$FIRMWARE_FLAVOR" = "oaf-test" ]; then
            echo "===== Required OAF test packages ====="
            for required_config in \
              CONFIG_PACKAGE_luci-app-oaf=y \
              CONFIG_PACKAGE_appfilter=y \
              CONFIG_PACKAGE_kmod-oaf=y
            do
              if ! grep -qx "$required_config" .config; then
                echo "ERROR: required OAF config missing: $required_config"
                exit 1
              fi
              echo "$required_config"
            done
          fi

          make download -j8

          find dl -size -1024c -exec ls -l {} \;
          find dl -size -1024c -exec rm -f {} \;

      - name: Compile firmware
        id: compile
        working-directory: openwrt
        run: |
          echo "$(nproc) thread compile"
          set -o pipefail

          if ! make -j"$(nproc)" 2>&1 | tee build.log; then
            echo "Parallel build failed; retrying with verbose single-thread output"
            make -j1 V=s 2>&1 | tee -a build.log
          fi

          echo "status=success" >> "$GITHUB_OUTPUT"

          DEVICE_NAME="$(
            grep '^CONFIG_TARGET.*DEVICE.*=y' .config |
              sed -r 's/.*DEVICE_(.*)=y/\1/'
          )"

          if [ -n "$DEVICE_NAME" ]; then
            echo "DEVICE_NAME=_$DEVICE_NAME" >> "$GITHUB_ENV"
          fi

          echo "FILE_DATE=_$(date '+%Y%m%d%H%M')" >> "$GITHUB_ENV"

      - name: Upload failed build log
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: build-log-${{ github.run_id }}
          path: openwrt/build.log
          retention-days: 7
          if-no-files-found: warn

      - name: Check disk usage
        if: always()
        run: df -hT

      - name: Upload complete bin directory
        if: >-
          steps.compile.outputs.status == 'success' &&
          env.UPLOAD_BIN_DIR == 'true'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: OpenWrt_bin${{ env.DEVICE_NAME }}${{ env.FILE_DATE }}
          path: openwrt/bin
          compression-level: 6
          retention-days: 7

      - name: Organize release files
        id: organize
        if: >-
          steps.compile.outputs.status == 'success' &&
          env.UPLOAD_FIRMWARE == 'true'
        run: |
          TARGET_BOARD="$(sed -n 's/^CONFIG_TARGET_BOARD="\([^"]*\)"/\1/p' openwrt/.config)"
          TARGET_SUBTARGET="$(sed -n 's/^CONFIG_TARGET_SUBTARGET="\([^"]*\)"/\1/p' openwrt/.config)"

          if [ -z "$TARGET_BOARD" ] || [ -z "$TARGET_SUBTARGET" ]; then
            echo "ERROR: Unable to determine target directory from .config"
            exit 1
          fi

          case "$TARGET_BOARD/$TARGET_SUBTARGET" in
            *[!A-Za-z0-9._/-]*)
              echo "ERROR: Unsafe target path"
              exit 1
              ;;
          esac

          FIRMWARE_DIR="$GITHUB_WORKSPACE/openwrt/bin/targets/$TARGET_BOARD/$TARGET_SUBTARGET"
          RELEASE_DIR="$GITHUB_WORKSPACE/release-output"

          if [ ! -d "$FIRMWARE_DIR" ]; then
            echo "ERROR: Firmware directory not found: $FIRMWARE_DIR"
            exit 1
          fi

          mkdir -p "$RELEASE_DIR"
          find "$FIRMWARE_DIR" -maxdepth 1 -type f -exec cp -t "$RELEASE_DIR" -- {} +
          cp openwrt/.config "$RELEASE_DIR/config.seed"

          MANIFEST_FILE="$(find "$RELEASE_DIR" -maxdepth 1 -type f -name '*.manifest' -print -quit)"
          if [ -n "$MANIFEST_FILE" ]; then
            cp "$MANIFEST_FILE" "$RELEASE_DIR/packages.manifest"
          else
            sed -n 's/^CONFIG_PACKAGE_\(.*\)=y$/\1/p' openwrt/.config |
              sort > "$RELEASE_DIR/packages.manifest"
          fi

          {
            echo "immortalwrt=$(git -C openwrt rev-parse HEAD)"
            echo "lucky=$(git -C openwrt/package/lucky rev-parse HEAD)"
            echo "nikki=$(git -C openwrt/package/nikki rev-parse HEAD)"
            if [ -d openwrt/package/OpenAppFilter/.git ]; then
              echo "openappfilter=$(git -C openwrt/package/OpenAppFilter rev-parse HEAD)"
            fi
          } > "$RELEASE_DIR/source-versions.txt"

          (
            cd "$RELEASE_DIR"
            find . -maxdepth 1 -type f ! -name sha256sums -printf '%P\0' |
              sort -z |
              xargs -0 sha256sum > sha256sums
          )

          echo "FIRMWARE_DIR=$FIRMWARE_DIR" >> "$GITHUB_ENV"
          echo "RELEASE_DIR=$RELEASE_DIR" >> "$GITHUB_ENV"
          echo "status=success" >> "$GITHUB_OUTPUT"

          echo "===== Release files ====="
          find "$RELEASE_DIR" -maxdepth 1 -type f -printf '%f\n' | sort

      - name: Generate release information
        id: release_info
        if: >-
          steps.organize.outputs.status == 'success' &&
          env.UPLOAD_RELEASE == 'true'
        run: |
          if [ "$FIRMWARE_FLAVOR" = "oaf-test" ]; then
            RELEASE_TAG="immortalwrt-oaf-test-$(date '+%Y.%m.%d-%H%M')"
            RELEASE_TITLE="ImmortalWrt OAF 测试固件"
            RELEASE_PLUGINS="Nikki、Lucky、OpenAppFilter（OAF）"
            RELEASE_PRERELEASE=true
          else
            RELEASE_TAG="immortalwrt-nikki-$(date '+%Y.%m.%d-%H%M')"
            RELEASE_TITLE="ImmortalWrt 固件"
            RELEASE_PLUGINS="Nikki、Lucky"
            RELEASE_PRERELEASE=false
          fi

          echo "release_tag=$RELEASE_TAG" >> "$GITHUB_OUTPUT"
          echo "release_prerelease=$RELEASE_PRERELEASE" >> "$GITHUB_OUTPUT"

          {
            echo "## $RELEASE_TITLE"
            echo
            echo "- 设备：JDCloud RE-CS-07"
            echo "- 源码：VIKINGYFY/immortalwrt"
            echo "- 分支：$REPO_BRANCH"
            echo "- 构建类型：$FIRMWARE_FLAVOR"
            echo "- 编译时间：$(date '+%Y-%m-%d %H:%M:%S %Z')"
            echo "- 默认地址：192.168.0.1"
            echo "- 插件：$RELEASE_PLUGINS"
            if [ "$FIRMWARE_FLAVOR" = "oaf-test" ]; then
              echo "- OAF 默认状态：已安装但未启用，未预设设备或拦截规则"
              echo "- 加速默认状态：保留 NSS/ECM，软件及硬件 Flow Offloading 关闭"
              echo
              echo "> 此版本为预发布测试固件，请先备份配置；仅用于验证《梦幻西游》识别和按设备拦截。"
            fi
            echo
            echo "### 源码版本"
            echo '```text'
            cat "$RELEASE_DIR/source-versions.txt"
            echo '```'
            echo
            echo "请使用 sha256sums 校验下载文件。"
          } > "$RELEASE_DIR/release.txt"

          (
            cd "$RELEASE_DIR"
            sha256sum release.txt >> sha256sums
          )

      - name: Upload firmware bundle
        if: steps.organize.outputs.status == 'success'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: release-bundle-${{ github.run_id }}
          path: release-output
          compression-level: 6
          retention-days: 14
          if-no-files-found: error

  release:
    needs: build
    if: needs.build.outputs.release_tag != ''
    runs-on: ubuntu-22.04
    permissions:
      contents: write

    steps:
      - name: Download firmware bundle
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: release-bundle-${{ github.run_id }}
          path: release-output

      - name: Publish GitHub release
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3
        with:
          tag_name: ${{ needs.build.outputs.release_tag }}
          name: ${{ needs.build.outputs.release_tag }}
          prerelease: ${{ needs.build.outputs.release_prerelease == 'true' }}
          body_path: release-output/release.txt
          files: release-output/*
          fail_on_unmatched_files: true

  cleanup:
    needs: release
    if: always() && needs.release.result == 'success'
    runs-on: ubuntu-22.04
    permissions:
      contents: write
      actions: write

    steps:
      - name: Remove old releases
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if [ "$FIRMWARE_FLAVOR" = "oaf-test" ]; then
            RELEASE_PREFIX="immortalwrt-oaf-test-"
          else
            RELEASE_PREFIX="immortalwrt-nikki-"
          fi

          gh release list \
            --repo "$GITHUB_REPOSITORY" \
            --limit 100 \
            --json tagName \
            --jq "map(select(.tagName | startswith(\"$RELEASE_PREFIX\"))) | .[3:] | .[].tagName" |
          while IFS= read -r tag; do
            [ -n "$tag" ] || continue
            echo "Deleting old release: $tag"
            gh release delete "$tag" \
              --repo "$GITHUB_REPOSITORY" \
              --cleanup-tag \
              --yes
          done

      - name: Remove old workflow runs
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh run list \
            --repo "$GITHUB_REPOSITORY" \
            --workflow "$GITHUB_WORKFLOW" \
            --limit 100 \
            --json databaseId,status,createdAt \
            --jq '
              map(select(.status == "completed"))
              | sort_by(.createdAt)
              | reverse
              | .[2:]
              | .[].databaseId
            ' |
          while IFS= read -r run_id; do
            [ -n "$run_id" ] || continue
            echo "Deleting old workflow run: $run_id"
            gh run delete "$run_id" --repo "$GITHUB_REPOSITORY"
          done
